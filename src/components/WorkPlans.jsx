export default function WorkPlans({ plans, subjects, onToggle, onRemove, onShare, canShare }) {
  const names = Object.fromEntries(subjects.map((x) => [x.id, x.short || x.name || x.code]))
  if (!plans.length) return null
  return <section className="mt-6" aria-labelledby="workplans-heading"><h2 id="workplans-heading" className="font-display text-lg font-semibold">Lagrede arbeidsplaner</h2><div className="mt-3 space-y-3">{plans.map((plan) => <article key={plan.id} className="rounded-lg border border-line bg-surface p-5"><div className="flex flex-wrap justify-between gap-2"><div><h3 className="font-semibold">{plan.title}</h3><p className="text-xs text-muted">{names[plan.subjectId] || 'Uten fag'}</p></div><div className="flex gap-2"><button onClick={() => onRemove(plan.id)} className="btn-ghost !min-h-8 !px-2 !py-1 text-xs">Slett</button>{canShare && <button onClick={() => onShare(plan)} className="btn-ghost !min-h-8 !px-2 !py-1 text-xs">Del</button>}</div></div>{plan.summary && <p className="mt-2 text-sm text-muted">{plan.summary}</p>}<ol className="mt-3 space-y-2">{plan.steps.map((step, index) => <li key={step.id} className="flex gap-2 text-sm"><input type="checkbox" checked={step.completed} onChange={() => onToggle(plan.id, step.id)} aria-label={`Fullfør ${step.title}`} /><span className={step.completed ? 'line-through text-muted' : ''}>{index + 1}. {step.title}</span></li>)}</ol></article>)}</div></section>
}
